import { createContext } from "react";

const rowsContext = createContext(null);
const colContext = createContext(null);
const divContext = createContext(null);

export { rowsContext, colContext, divContext };
