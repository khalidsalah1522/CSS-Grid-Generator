import { createContext } from "react";

const colsContext = createContext(null);
const rowsContext = createContext(null);
const newDivContext = createContext(null);

export { colsContext, rowsContext, newDivContext };
